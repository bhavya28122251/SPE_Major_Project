apiVersion: v1
kind: Service
metadata:
  name: api-gateway-svc
  labels:
    app: api-gateway-app
spec:
  type: ClusterIP
  selector:
    app: api-gateway-app
  ports:
    - port: 80
      targetPort: 8085